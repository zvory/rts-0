// Shared by every client-side authored-map producer and consumer. The Rust loader mirrors this
// value as CURRENT_MAP_VERSION because authored maps cross the client/server boundary as JSON.
export const CURRENT_AUTHORED_MAP_VERSION = 10;
