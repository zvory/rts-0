// Mirrors the server-assigned player palette. The client uses it only as a
// fallback when an authoritative player color is unavailable.
export const PLAYER_PALETTE = Object.freeze([
  "#0072b2", "#d55e00", "#009e73", "#cc79a7",
  "#56b4e9", "#e69f00", "#f0e442", "#7e57c2",
]);
