# RTS Simulation WASM

Generated browser assets for `rts-sim-wasm` are written here by:

```bash
scripts/build-sim-wasm.sh
```

The directory is served with the rest of `client/` during local development. Generated `.wasm`,
`.js`, and TypeScript declaration files are intentionally ignored.
