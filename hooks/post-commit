#!/usr/bin/env bash
# Opportunistically clean merged worktrees after commits on main.
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
"$repo_root/scripts/cleanup-worktrees.sh" --auto || {
  echo "post-commit: worktree cleanup skipped after an error" >&2
}
