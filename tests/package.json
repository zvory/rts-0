{
  "name": "rts-tests",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for the RTS game (run against a live server on :8080).",
  "scripts": {
    "test:server": "node server_integration.mjs",
    "test:client": "node client_smoke.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^23"
  }
}
