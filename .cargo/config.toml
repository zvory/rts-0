# Project Cargo configuration intentionally does not set `build.target-dir`.
#
# Multiple agents build this repo from parallel git worktrees. A single static
# target dir makes final binaries and test artifacts shared mutable state across
# unrelated branches. Test/helper scripts choose an isolated per-worktree target
# dir instead.
