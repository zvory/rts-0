[package]
name = "rts-protocol"
version = "0.1.0"
edition = "2021"
description = "WebSocket wire protocol and compact transport encoding for Bewegungskrieg"

[dependencies]
rts-contract = { path = "../contract" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
