{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default shell permissions for the local macOS cursor spike window.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
