# Quick Fix Notes

- Player-controlled workers should not run away when attacked; only AI workers should receive the direct-hit retreat behavior.
- When worker placement is invalid, the mining preview should show a dashed line that is crossed out, and the far resource marker should be an `X` instead of a circle.
- Add a way to give up from a settings menu so players can reach the score screen without waiting to be eliminated.
