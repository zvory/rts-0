{
  "name": "rts-agent-tools",
  "private": true,
  "type": "module",
  "description": "Repository-owned Node dependencies for RTS development tools and browser tests.",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "check:lab-interact-types": "tsc --project scripts/lab-interact/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22",
    "puppeteer-core": "^23",
    "typescript": "^5.8"
  }
}
